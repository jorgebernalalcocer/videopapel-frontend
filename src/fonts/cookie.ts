'use client'

import { Cookie } from 'next/font/google'

export const cookie = Cookie({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-cookie',
})

export const cookieFontStack = '"Cookie", cursive'
