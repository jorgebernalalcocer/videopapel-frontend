'use client'

import { Borel } from 'next/font/google'

export const borel = Borel({
  subsets: ['latin'],
  weight: '400',
  variable: '--font-borel',
})

export const borelFontStack = '"Borel", cursive'
